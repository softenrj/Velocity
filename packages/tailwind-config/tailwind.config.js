/** @type {import('tailwindcss').Config} */
module.exports = {
    theme: {
        extend: {
            fontFamily: {
                sans: ['var(--font-geist-sans)', 'serif'],
                lora: ['var(--font-lora)', 'serif'],
            },
        },
    },
    plugins: [],
};
